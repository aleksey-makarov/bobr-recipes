# The fetch-request schema these recipes emit. `bin/bobr-fetch.sh` compares it
# against what the `bobr-fetch` on PATH reports, the same handshake
# `bin/bobr-build.sh` does with `bobr` over request-schema.ncl.
"bobr-fetch-request-v1"
