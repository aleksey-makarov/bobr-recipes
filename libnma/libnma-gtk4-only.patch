--- a/meson_options.txt
+++ b/meson_options.txt
@@ -1,3 +1,4 @@
+option('libnma_gtk3', type: 'boolean', value: true, description: 'build library libnma for use with GTK3')
 option('libnma_gtk4', type: 'boolean', value: false, description: 'build library libnma-gtk4 for use with GTK4')
 option('gcr', type: 'boolean', value: true, description: 'Enable advanced certificate chooser.')
 option('more_asserts', type: 'string', value: '0', description: 'Enable more assertions for debugging.')
--- a/meson.build
+++ b/meson.build
@@ -72,6 +72,7 @@
   common_ldflags += test_ldflags
 endif
 
+enable_libnma_gtk3 = get_option('libnma_gtk3')
 enable_libnma_gtk4 = get_option('libnma_gtk4')
 if enable_libnma_gtk4
   gtk4_ext_dep = dependency('gtk4', version: '>= 4.0')
@@ -156,13 +157,19 @@
 # Check for gtk+
 gtk_req_version = '>= 3.12'
 
-gtk_dep = declare_dependency(
-  dependencies: dependency('gtk+-3.0', version: gtk_req_version),
-  compile_args: [
-    '-DGDK_VERSION_MIN_REQUIRED=GDK_VERSION_3_12',
-    '-DGDK_VERSION_MAX_ALLOWED=GDK_VERSION_3_12'
-  ]
-)
+if enable_libnma_gtk3
+  gtk_dep = declare_dependency(
+    dependencies: dependency('gtk+-3.0', version: gtk_req_version),
+    compile_args: [
+      '-DGDK_VERSION_MIN_REQUIRED=GDK_VERSION_3_12',
+      '-DGDK_VERSION_MAX_ALLOWED=GDK_VERSION_3_12'
+    ]
+  )
+else
+  # GTK4-only build: the shared utils/nma-ws sources still reference gtk_dep, so
+  # point it at the GTK4 toolkit dependency instead of GTK3.
+  gtk_dep = gtk4_dep
+endif
 
 # API documentation
 nm_req_version = '>= 1.7'
--- a/src/meson.build
+++ b/src/meson.build
@@ -91,30 +91,32 @@
 
 symbol_map = join_paths(meson.current_source_dir(), 'libnma.ver')
 
-nma_deps = [ deps, gtk_dep ]
-nma_sources = [ gir_sources, built_sources ]
-if enable_gcr
-  nma_sources += pkcs11_sources
-  nma_deps += gcr_dep
-endif
+if enable_libnma_gtk3
+  nma_deps = [ deps, gtk_dep ]
+  nma_sources = [ gir_sources, built_sources ]
+  if enable_gcr
+    nma_sources += pkcs11_sources
+    nma_deps += gcr_dep
+  endif
+
+  libnma = shared_library(
+    'nma',
+    sources: nma_sources,
+    version: libversion,
+    include_directories: incs,
+    dependencies: nma_deps,
+    c_args: cflags,
+    link_args: '-Wl,--version-script,' + symbol_map,
+    link_depends: symbol_map,
+    install: true,
+    install_dir: nma_libdir
+  )
 
-libnma = shared_library(
-  'nma',
-  sources: nma_sources,
-  version: libversion,
-  include_directories: incs,
-  dependencies: nma_deps,
-  c_args: cflags,
-  link_args: '-Wl,--version-script,' + symbol_map,
-  link_depends: symbol_map,
-  install: true,
-  install_dir: nma_libdir
-)
-
-libnma_dep = declare_dependency(
-  link_with: libnma,
-  include_directories: src_inc
-)
+  libnma_dep = declare_dependency(
+    link_with: libnma,
+    include_directories: src_inc
+  )
+endif
 
 if enable_libnma_gtk4
   # This is all a bit clumsy, but what can you do...
@@ -196,17 +198,19 @@
 
 subdir('tests')
 
-pkg.generate(
-  libraries: libnma,
-  version: nma_version,
-  name: 'libnma',
-  description: 'NetworkManager UI utilities (libnm version)',
-  filebase: 'libnma',
-  subdirs: 'libnma',
-  requires: 'libnm',
-  variables: 'exec_prefix=' + nma_prefix,
-  install_dir: join_paths(nma_libdir, 'pkgconfig')
-)
+if enable_libnma_gtk3
+  pkg.generate(
+    libraries: libnma,
+    version: nma_version,
+    name: 'libnma',
+    description: 'NetworkManager UI utilities (libnm version)',
+    filebase: 'libnma',
+    subdirs: 'libnma',
+    requires: 'libnm',
+    variables: 'exec_prefix=' + nma_prefix,
+    install_dir: join_paths(nma_libdir, 'pkgconfig')
+  )
+endif
 
 if enable_libnma_gtk4
   pkg.generate(
@@ -226,19 +230,21 @@
   gir_dir = join_paths(nma_datadir, 'gir-' + nma_gir_version)
   typelib_dir = join_paths(nma_libdir, 'girepository-' + nma_gir_version)
 
-  libnma_gir = gnome.generate_gir(
-    libnma,
-    sources: gir_sources + gir_headers,
-    nsversion: nma_gir_version,
-    namespace: 'NMA',
-    includes: [ 'Gtk-3.0', 'NM-1.0' ],
-    identifier_prefix: nma_gir_prefix,
-    symbol_prefix: nma_gir_prefix.to_lower(),
-    export_packages: 'libnma',
-    install: true,
-    install_dir_gir: gir_dir,
-    install_dir_typelib: typelib_dir
-  )
+  if enable_libnma_gtk3
+    libnma_gir = gnome.generate_gir(
+      libnma,
+      sources: gir_sources + gir_headers,
+      nsversion: nma_gir_version,
+      namespace: 'NMA',
+      includes: [ 'Gtk-3.0', 'NM-1.0' ],
+      identifier_prefix: nma_gir_prefix,
+      symbol_prefix: nma_gir_prefix.to_lower(),
+      export_packages: 'libnma',
+      install: true,
+      install_dir_gir: gir_dir,
+      install_dir_typelib: typelib_dir
+    )
+  endif
 
   if enable_libnma_gtk4
     libnma_gtk4_gir = gnome.generate_gir(
@@ -258,12 +264,14 @@
 endif
 
 if enable_vapi
-  gnome.generate_vapi(
-    'libnma',
-    sources: libnma_gir[0],
-    packages: [ 'libnm', 'gtk+-3.0', 'gio-2.0' ],
-    install: true,
-  )
+  if enable_libnma_gtk3
+    gnome.generate_vapi(
+      'libnma',
+      sources: libnma_gir[0],
+      packages: [ 'libnm', 'gtk+-3.0', 'gio-2.0' ],
+      install: true,
+    )
+  endif
 
   if enable_libnma_gtk4
     gnome.generate_vapi(
--- a/src/tests/meson.build
+++ b/src/tests/meson.build
@@ -10,14 +10,16 @@
 ]
 
 foreach tst : tests
-  executable(
-    tst,
-    tst + '.c',
-    include_directories: incs,
-    dependencies: [ gtk_dep, libnm_dep, libnma_dep ],
-    c_args: cflags,
-    install: false
-  )
+  if enable_libnma_gtk3
+    executable(
+      tst,
+      tst + '.c',
+      include_directories: incs,
+      dependencies: [ gtk_dep, libnm_dep, libnma_dep ],
+      c_args: cflags,
+      install: false
+    )
+  endif
 
   if enable_libnma_gtk4
     executable(
