# sshd privilege-separation directory. Must exist, be owned by root and not be
# group/world-writable, or sshd refuses to start. On the volatile rootfs it is
# (re)created every boot by systemd-tmpfiles.
d /var/empty 0755 root root -
