# gdm's home/state directory, owned by the gdm user (created by sysusers, which
# does not create home dirs). gdm keeps greeter state here and refuses to start
# if it is missing or wrongly owned.
#Type Path         Mode UID GID Age Argument
d     /var/lib/gdm 0711 gdm gdm -   -
