diff --git a/src/options.cpp b/src/options.cpp
index 7acd850..9dd13db 100644
--- a/src/options.cpp
+++ b/src/options.cpp
@@ -22,5 +22,6 @@
  */
 
+#include <cstdlib>
 #include <cstring>
 #include <cstdio>
 #include <getopt.h>
@@ -29,10 +30,17 @@
 #include "options.h"
 #include "util.h"
 
+static std::string
+data_path_from_environment()
+{
+    const char *path = std::getenv("GLMARK2_DATA_PATH");
+    return path && path[0] ? std::string(path) : std::string(GLMARK_DATA_PATH);
+}
+
 std::vector<std::string> Options::benchmarks;
 std::vector<std::string> Options::benchmark_files;
 bool Options::validate = false;
-std::string Options::data_path = std::string(GLMARK_DATA_PATH);
+std::string Options::data_path = data_path_from_environment();
 Options::FrameEnd Options::frame_end = Options::FrameEndDefault;
 Options::SwapMode Options::swap_mode = Options::SwapModeDefault;
 std::pair<int,int> Options::size(800, 600);
diff --git a/src/gl-state-egl.cpp b/src/gl-state-egl.cpp
index 5d9d88f..acb57e3 100644
--- a/src/gl-state-egl.cpp
+++ b/src/gl-state-egl.cpp
@@ -572,7 +572,11 @@ GLStateEGL::gotValidDisplay()
 #endif
 #elif GLMARK2_USE_GL
     EGLenum apiType(EGL_OPENGL_API);
-    std::initializer_list<const char *> libNames = { "libGL.so", "libGL.so.1" };
+#if defined(WIN32)
+    std::initializer_list<const char *> libNames = { "opengl32.dll" };
+#else
+    std::initializer_list<const char *> libNames = { "libOpenGL.so.0", "libOpenGL.so" };
+#endif
     if (!GLAD_EGL_VERSION_1_4) {
         Log::error("EGL version %d.%d does not support the OpenGL API\n",
                    egl_major, egl_minor);
