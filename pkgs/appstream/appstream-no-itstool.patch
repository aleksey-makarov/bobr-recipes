Build without itstool.

data/meson.build runs the appstreamcli metainfo through itstool, which needs
python libxml2 bindings we do not ship. The translated metainfo does not matter
to our consumers (libadwaita / gnome-control-center link libappstream), so
install the generated untranslated file. Documentation and man pages are
disabled through upstream Meson options.

--- a/data/meson.build
+++ b/data/meson.build
@@ -26,11 +26,12 @@ metainfo_with_relinfo = custom_target('gen-output',
     command : [ascli_exe, 'news-to-metainfo', '--limit=6', '@INPUT0@', '@INPUT1@', '@OUTPUT@']
 )
 
-metainfo_i18n = i18n.itstool_join(
-    input:  metainfo_with_relinfo,
+# itstool is not available in this build; install the untranslated metainfo
+# for the appstreamcli tool instead of running itstool_join over it.
+metainfo_i18n = custom_target('cli-metainfo',
+    input: metainfo_with_relinfo,
     output: 'org.freedesktop.appstream.cli.metainfo.xml',
-    mo_targets: i18n_result[0],
-    its_files: [join_paths(meson.current_source_dir(), 'its', 'metainfo.its')],
+    command: ['cp', '@INPUT@', '@OUTPUT@'],
     install: true,
     install_dir: metainfo_dir,
 )
